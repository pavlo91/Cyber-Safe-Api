extends ../layout.pug

block head
  title You have been invited as an athlete in #{teamName}

block body
  p You have been invited as an athlete in #{teamName}
  p
    | You can either 
    a(href=acceptUrl) accept
    |  or 
    a(href=declineUrl) decline
    |  the invitation.
