extends _layout.pug

block head
  title You have been invited as a staff at #{appName}

block body
  p You have been invited as a staff at #{appName}.
  br
  a.button(href=webURL + "/invite/" + token) Accept Invite
