extends _layout.pug

block head
  title You have been invited as parent of #{childName}

block body
  p You have been invited as parent of #{childName}.
  br
  a.button(href=webURL + "/invite/" + token) Accept Invite
