extends layout.pug

block head
  title You have been invited as parent of #{childName}

block body
  p
    | You have been invited as parent of #{childName}. You can either 
    a(href=acceptURL) accept
    |  or 
    a(href=declineURL) decline
    |  the invitation.
