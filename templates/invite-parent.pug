extends layout.pug

block head
  title You have been invited as parent of #{childName}

block body
  p
    | You have been invited as parent of #{childName}. Click 
    a(href=url) here
    |  to view invitation.
